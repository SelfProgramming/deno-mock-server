export function executeHelp(): void {
  console.log(`
    Welcome to denomock cli
    
    Commands:
    
      denomock help - show this screen
      
      denomock start - start new mocked http server
  `);
}
